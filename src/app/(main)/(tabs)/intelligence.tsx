import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function LiveIntelligenceScreen() {
  return (
    <SafeAreaView>
      <Text>LiveIntelligenceScreen</Text>
    </SafeAreaView>
  );
}
